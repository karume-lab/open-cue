diff --git a/android/build.gradle b/android/build.gradle
index 748b799554aa0f3432432a73ea93435d60a581dc..e3dab827940ba1af593d79e57a8ac9547485c524 100644
--- a/android/build.gradle
+++ b/android/build.gradle
@@ -240,6 +240,8 @@ dependencies {
         implementation "androidx.media3:media3-exoplayer:$media3_version"
     }
 
+    implementation "io.github.anilbeesetti:nextlib-media3ext:1.8.0-0.9.0"
+
     if (ExoplayerDependencies["useExoplayerSmoothStreaming"]) {
         // For Smooth Streaming playback support with ExoPlayer
         if (media3_buildFromSource) {
diff --git a/android/src/main/java/com/brentvatne/exoplayer/ReactExoplayerView.java b/android/src/main/java/com/brentvatne/exoplayer/ReactExoplayerView.java
index e16ac96d8a89055ee6e950777ce73388d22061ee..0be346ad91787b1dde82f24200c6f300fa1f490e 100644
--- a/android/src/main/java/com/brentvatne/exoplayer/ReactExoplayerView.java
+++ b/android/src/main/java/com/brentvatne/exoplayer/ReactExoplayerView.java
@@ -59,7 +59,10 @@ import androidx.media3.datasource.DefaultDataSource;
 import androidx.media3.datasource.HttpDataSource;
 import androidx.media3.exoplayer.DefaultLoadControl;
 import androidx.media3.exoplayer.DefaultRenderersFactory;
+import androidx.media3.exoplayer.ExoPlaybackException;
 import androidx.media3.exoplayer.ExoPlayer;
+import androidx.media3.exoplayer.Renderer;
+import androidx.media3.exoplayer.RenderersFactory;
 import androidx.media3.exoplayer.dash.DashMediaSource;
 import androidx.media3.exoplayer.dash.DashUtil;
 import androidx.media3.exoplayer.dash.DefaultDashChunkSource;
@@ -102,6 +105,8 @@ import androidx.media3.exoplayer.upstream.CmcdConfiguration;
 import androidx.media3.exoplayer.upstream.DefaultAllocator;
 import androidx.media3.exoplayer.upstream.DefaultBandwidthMeter;
 import androidx.media3.exoplayer.util.EventLogger;
+import io.github.anilbeesetti.nextlib.media3ext.ffdecoder.FfmpegVideoRenderer;
+import io.github.anilbeesetti.nextlib.media3ext.ffdecoder.FfmpegLibrary;
 import androidx.media3.extractor.metadata.emsg.EventMessage;
 import androidx.media3.extractor.metadata.id3.Id3Frame;
 import androidx.media3.extractor.metadata.id3.TextInformationFrame;
@@ -217,6 +222,8 @@ public class ReactExoplayerView extends FrameLayout implements
     private float audioVolume = 1f;
     private int maxBitRate = 0;
     private boolean hasDrmFailed = false;
+    private boolean hasSoftwareDecoderRetry = false;
+    private boolean forceSoftwareVideoDecoder = false;
     private boolean isUsingContentResolution = false;
     private boolean selectTrackWhenReady = false;
     private final Handler mainHandler;
@@ -730,11 +737,37 @@ public class ReactExoplayerView extends FrameLayout implements
             this.bandwidthMeter = config.getBandwidthMeter();
         }
 
-        DefaultRenderersFactory renderersFactory =
-                new DefaultRenderersFactory(getContext())
-                        .setExtensionRendererMode(DefaultRenderersFactory.EXTENSION_RENDERER_MODE_OFF)
-                        .setEnableDecoderFallback(true)
-                        .forceEnableMediaCodecAsynchronousQueueing();
+        DefaultRenderersFactory defaultRenderersFactory = new DefaultRenderersFactory(getContext());
+        defaultRenderersFactory.setExtensionRendererMode(DefaultRenderersFactory.EXTENSION_RENDERER_MODE_ON);
+        defaultRenderersFactory.setEnableDecoderFallback(true);
+        defaultRenderersFactory.forceEnableMediaCodecAsynchronousQueueing();
+
+        RenderersFactory renderersFactory = (eventHandler, videoRendererEventListener, audioRendererEventListener, textRendererOutput, metadataRendererOutput) -> {
+            Renderer[] renderers = defaultRenderersFactory.createRenderers(eventHandler, videoRendererEventListener, audioRendererEventListener, textRendererOutput, metadataRendererOutput);
+            if (forceSoftwareVideoDecoder) {
+                // Use the FFmpeg software decoder as the sole video renderer. This is
+                // enabled after a hardware decoder fails to decode a format it
+                // advertised as supported (e.g. 10-bit HEVC Main10).
+                try {
+                    if (FfmpegLibrary.isAvailable() && renderers.length > 0) {
+                        renderers[0] = new FfmpegVideoRenderer(0L, eventHandler, videoRendererEventListener, C.TRACK_TYPE_VIDEO);
+                    }
+                } catch (Throwable ignored) {
+                }
+            } else {
+                try {
+                    if (FfmpegLibrary.isAvailable() && renderers.length > 0) {
+                        Renderer[] withFfmpegFallback = new Renderer[renderers.length + 1];
+                        System.arraycopy(renderers, 0, withFfmpegFallback, 0, 1);
+                        withFfmpegFallback[1] = new FfmpegVideoRenderer(0L, eventHandler, videoRendererEventListener, C.TRACK_TYPE_VIDEO);
+                        System.arraycopy(renderers, 1, withFfmpegFallback, 2, renderers.length - 1);
+                        renderers = withFfmpegFallback;
+                    }
+                } catch (Throwable ignored) {
+                }
+            }
+            return renderers;
+        };
 
         DefaultMediaSourceFactory mediaSourceFactory;
         
@@ -1587,25 +1620,33 @@ public class ReactExoplayerView extends FrameLayout implements
 
     private ArrayList<VideoTrack> getVideoTrackInfo() {
         ArrayList<VideoTrack> videoTracks = new ArrayList<>();
-        if (trackSelector == null) {
+        if (trackSelector == null || player == null) {
             // Likely player is unmounting so no video tracks are available anymore
             return videoTracks;
         }
         MappingTrackSelector.MappedTrackInfo info = trackSelector.getCurrentMappedTrackInfo();
-        int index = getTrackRendererIndex(C.TRACK_TYPE_VIDEO);
-        if (info == null || index == C.INDEX_UNSET) {
+        if (info == null) {
             return videoTracks;
         }
 
-        TrackGroupArray groups = info.getTrackGroups(index);
-        for (int i = 0; i < groups.length; ++i) {
-            TrackGroup group = groups.get(i);
+        // The FFmpeg software decoder is appended as an extra video renderer, so
+        // a codec with no hardware decoder (e.g. HEVC on some devices) is mapped
+        // there instead of the first video renderer. Collect tracks from every
+        // video renderer, otherwise those streams report zero video tracks.
+        for (int rendererIndex = 0; rendererIndex < player.getRendererCount(); rendererIndex++) {
+            if (player.getRendererType(rendererIndex) != C.TRACK_TYPE_VIDEO) {
+                continue;
+            }
+            TrackGroupArray groups = info.getTrackGroups(rendererIndex);
+            for (int i = 0; i < groups.length; ++i) {
+                TrackGroup group = groups.get(i);
 
-            for (int trackIndex = 0; trackIndex < group.length; trackIndex++) {
-                Format format = group.getFormat(trackIndex);
-                if (isFormatSupported(format)) {
-                    VideoTrack videoTrack = exoplayerVideoTrackToGenericVideoTrack(format, trackIndex);
-                    videoTracks.add(videoTrack);
+                for (int trackIndex = 0; trackIndex < group.length; trackIndex++) {
+                    Format format = group.getFormat(trackIndex);
+                    if (isFormatSupported(format)) {
+                        VideoTrack videoTrack = exoplayerVideoTrackToGenericVideoTrack(format, trackIndex);
+                        videoTracks.add(videoTrack);
+                    }
                 }
             }
         }
@@ -1970,6 +2011,25 @@ public class ReactExoplayerView extends FrameLayout implements
                     return;
                 }
                 break;
+            case PlaybackException.ERROR_CODE_DECODING_FAILED:
+                // Some hardware decoders advertise a codec (e.g. 10-bit HEVC Main10)
+                // but fail mid-decode. Retry once with the FFmpeg software decoder so
+                // the video can still be played; if that also fails the error below is
+                // surfaced to the JS layer as usual.
+                if (!hasSoftwareDecoderRetry
+                        && FfmpegLibrary.isAvailable()
+                        && e instanceof ExoPlaybackException
+                        && ((ExoPlaybackException) e).rendererIndex == getTrackRendererIndex(C.TRACK_TYPE_VIDEO)) {
+                    hasSoftwareDecoderRetry = true;
+                    forceSoftwareVideoDecoder = true;
+                    DebugLog.w(TAG, "Video decode failed, retrying with the FFmpeg software decoder");
+                    playerNeedsSource = true;
+                    updateResumePosition();
+                    releasePlayer();
+                    initializePlayer();
+                    return;
+                }
+                break;
             default:
                 break;
         }
@@ -2042,6 +2102,8 @@ public class ReactExoplayerView extends FrameLayout implements
             clearResumePosition();
             boolean isSourceEqual = source.isEquals(this.source);
             hasDrmFailed = false;
+            hasSoftwareDecoderRetry = false;
+            forceSoftwareVideoDecoder = false;
             this.source = source;
             final DataSource.Factory tmpMediaDataSourceFactory =
                     DataSourceUtil.getDefaultDataSourceFactory(this.themedReactContext, bandwidthMeter,
@@ -2392,6 +2454,11 @@ public class ReactExoplayerView extends FrameLayout implements
     }
 
     private boolean isFormatSupported(Format format) {
+        // When using the FFmpeg software decoder, include all tracks - FFmpeg has
+        // broad codec support and will fail at decode time if truly unsupported.
+        if (forceSoftwareVideoDecoder || FfmpegLibrary.isAvailable()) {
+            return true;
+        }
         int width = format.width == Format.NO_VALUE ? 0 : format.width;
         int height = format.height == Format.NO_VALUE ? 0 : format.height;
         float frameRate = format.frameRate == Format.NO_VALUE ? 0 : format.frameRate;
diff --git a/android/src/main/java/com/brentvatne/common/api/SubtitleStyle.kt b/android/src/main/java/com/brentvatne/common/api/SubtitleStyle.kt
--- a/android/src/main/java/com/brentvatne/common/api/SubtitleStyle.kt
+++ b/android/src/main/java/com/brentvatne/common/api/SubtitleStyle.kt
@@ -21,6 +21,14 @@
         private set
     var subtitlesFollowVideo = true
         private set
+    var hasColor = false
+        private set
+    var color = android.graphics.Color.TRANSPARENT
+        private set
+    var hasBackgroundColor = false
+        private set
+    var backgroundColor = android.graphics.Color.TRANSPARENT
+        private set
 
     companion object {
         private const val PROP_FONT_SIZE_TRACK = "fontSize"
@@ -30,6 +38,8 @@
         private const val PROP_PADDING_RIGHT = "paddingRight"
         private const val PROP_OPACITY = "opacity"
         private const val PROP_SUBTITLES_FOLLOW_VIDEO = "subtitlesFollowVideo"
+        private const val PROP_COLOR = "color"
+        private const val PROP_BACKGROUND_COLOR = "backgroundColor"
 
         @JvmStatic
         fun parse(src: ReadableMap?): SubtitleStyle {
@@ -41,6 +51,14 @@
             subtitleStyle.paddingRight = ReactBridgeUtils.safeGetInt(src, PROP_PADDING_RIGHT, 0)
             subtitleStyle.opacity = ReactBridgeUtils.safeGetFloat(src, PROP_OPACITY, 1f)
             subtitleStyle.subtitlesFollowVideo = ReactBridgeUtils.safeGetBool(src, PROP_SUBTITLES_FOLLOW_VIDEO, true)
+            src?.getString(PROP_COLOR)?.let {
+                subtitleStyle.color = android.graphics.Color.parseColor(it)
+                subtitleStyle.hasColor = true
+            }
+            src?.getString(PROP_BACKGROUND_COLOR)?.let {
+                subtitleStyle.backgroundColor = android.graphics.Color.parseColor(it)
+                subtitleStyle.hasBackgroundColor = true
+            }
             return subtitleStyle
         }
     }
diff --git a/android/src/main/java/com/brentvatne/exoplayer/ExoPlayerView.kt b/android/src/main/java/com/brentvatne/exoplayer/ExoPlayerView.kt
--- a/android/src/main/java/com/brentvatne/exoplayer/ExoPlayerView.kt
+++ b/android/src/main/java/com/brentvatne/exoplayer/ExoPlayerView.kt
@@ -13,6 +13,7 @@
 import androidx.media3.common.util.UnstableApi
 import androidx.media3.exoplayer.ExoPlayer
 import androidx.media3.ui.AspectRatioFrameLayout
+import androidx.media3.ui.CaptionStyleCompat
 import androidx.media3.ui.DefaultTimeBar
 import androidx.media3.ui.PlayerView
 import com.brentvatne.common.api.ResizeMode
@@ -118,6 +119,22 @@
                 subtitleView.setFixedTextSize(android.util.TypedValue.COMPLEX_UNIT_SP, style.fontSize.toFloat())
             }
 
+            if (style.hasColor || style.hasBackgroundColor) {
+                // A custom caption style must win over the track's embedded
+                // styling, otherwise the cue's own colors override the app's.
+                subtitleView.setApplyEmbeddedStyles(false)
+                subtitleView.setStyle(
+                    CaptionStyleCompat(
+                        if (style.hasColor) style.color else CaptionStyleCompat.USE_TRACK_COLOR_SETTINGS,
+                        if (style.hasBackgroundColor) style.backgroundColor else 0,
+                        0,
+                        CaptionStyleCompat.EDGE_TYPE_NONE,
+                        android.graphics.Color.TRANSPARENT,
+                        null
+                    )
+                )
+            }
+
             subtitleView.setPadding(
                 style.paddingLeft,
                 style.paddingTop,
diff --git a/lib/specs/VideoNativeComponent.d.ts b/lib/specs/VideoNativeComponent.d.ts
--- a/lib/specs/VideoNativeComponent.d.ts
+++ b/lib/specs/VideoNativeComponent.d.ts
@@ -118,6 +118,8 @@
     paddingRight?: WithDefault<Float, 0>;
     opacity?: WithDefault<Float, 1>;
     subtitlesFollowVideo?: WithDefault<boolean, true>;
+    color?: String;
+    backgroundColor?: String;
 }>;
 type OnLoadData = Readonly<{
     currentTime: Float;
diff --git a/lib/types/video.d.ts b/lib/types/video.d.ts
--- a/lib/types/video.d.ts
+++ b/lib/types/video.d.ts
@@ -166,6 +166,14 @@
     paddingRight?: number;
     opacity?: number;
     subtitlesFollowVideo?: boolean;
+    /**
+     * Android only. Text color of the native subtitle view, e.g. "#FFFFFF".
+     */
+    color?: string;
+    /**
+     * Android only. Background color (ARGB hex) behind the native subtitle text.
+     */
+    backgroundColor?: string;
 };
 export declare enum TextTrackType {
     SUBRIP = "application/x-subrip",
