import { View } from "react-native";
import { Text } from "@/components/ui/text";

const PlayerDetailScreen = () => {
  return (
    <View>
      <Text>PlayerDetailScreen</Text>
    </View>
  );
};

export default PlayerDetailScreen;
